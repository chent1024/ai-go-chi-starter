# Base AGENTS Template

This file mirrors the shared repository rules guidance used by `rules install`.
Copy it into a target repository only when you need a standalone editable base
template outside the generated runtime.

The canonical generated runtime still lives under `.orch/rules/`.
