# Base README Template

This is the shared explanatory template for a generated `.orch/rules/README.md`.

Use `rules install` to materialize the runtime files into a target repository.
Avoid editing generated runtime files by hand when the same change can be made
through `manifest.json` or `<target>/rules.env`.
