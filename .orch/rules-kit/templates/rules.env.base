# Base legacy rules env template.
#
# Prefer the manifest-based runtime under `.orch/rules/manifest.json` for
# new installs. Keep this template only for legacy compatibility flows.

REPO_ROOT="."
VERIFY_CMD="make verify"
STRICT_VERIFY_CMD="make verify-strict"
